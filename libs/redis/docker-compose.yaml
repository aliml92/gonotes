version: '3.0'

services:
  redis:
    image: redis:7
    container_name: redis
    restart: always
    volumes:
      - ./redis/redis.conf:/redis.conf:rw
      - ./redis/data:/data:rw
      - ./redis/users.acl:/etc/redis/users.acl:rw
    command:
      - /redis.conf  
    ports:
      - 6379:6379